function sum(a, b){
  return a + b;
}

//"test": "react-app-rewired test --env=jsdom"
// "test": "jest"
// "test": "react-scripts test --env=jsdom",
// "test": "mocha --require babel-register --require ./src/setup.js 'src/**/*spec.js'",

module.exports = sum;